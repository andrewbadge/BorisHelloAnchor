# Normalise line endings in the repository; check out with the platform default.
* text=auto

# Windows-only scripts and solution files keep CRLF on checkout.
*.ps1  text eol=crlf
*.sln  text eol=crlf

# Binary files.
*.ico  binary
*.png  binary
*.msi  binary
